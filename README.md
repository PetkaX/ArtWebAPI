# Article API

Простой REST API для управления статьями, тегами и секциями. Построен на **ASP.NET Core 8** с использованием **чистой архитектуры**, **Entity Framework Core** и **xUnit-тестами**.

> 🎯 Подходит для блогов, документации, новостных платформ.

## 📦 Технологии

- **ASP.NET Core 8** — Web API
- **Entity Framework Core 8** — ORM, миграции
- **PostgreSQL** — основная БД (рекомендуется)
- **C# 12** — `record`, `Primary Constructors`, `Required`, `ImplicitUsings`
- **xUnit + Moq** — модульные тесты
- **Docker + Testcontainers** — интеграционные тесты с реальной БД
- **Clean Architecture** — чёткое разделение слоёв

## 🗂️ Структура проекта
```
ArticleApi/ 
├── ArticleApi.Application/    # DTO, сервисы, интерфейсы 
├── ArticleApi.Domain/         # Сущности, Value Objects 
├── ArticleApi.Infrastructure/ # EF Core, репозитории, реализации 
├── ArticleApi.Api/            # Контроллеры, Startup, Program.cs 
├── ArticleApi.Tests.Unit/     # Юнит-тесты (xUnit, Moq) 
├── .gitignore 
├── README.md 
└── ArticleApi.sln
```

### 🔍 Назначение слоёв

| Слой | Ответственность |
|------|----------------|
| `Domain` | Чистые сущности, интерфейсы репозиториев |
| `Application` | DTO, бизнес-логика, сервисы, валидация |
| `Infrastructure` | Реализация репозиториев, EF Core, миграции |
| `Presentation` | Контроллеры, маршруты, Swagger, запуск приложения |
| `Tests` | Юнит- и интеграционные тесты с моками и Testcontainers |

```
ArticleApi.Domain — Чистая доменная модель.
Содержит сущности и абстракции (интерфейсы репозиториев)

ArticleApi.Application — Логика приложения.
Содержит Use Cases, DTOs, валидацию. Зависит от Domain

ArticleApi.Infrastructure — Доступ к данным.
Реализует IArticleRepository с помощью Entity Framework Core + PostgreSQL.

ArticleApi.Presentation — ASP.NET Core Web API.
Точка входа Application.
```

## 🚀 Запуск проекта

### 1. Установка зависимостей

```
bash
dotnet restore
```

### 2. Настройка БД
Скопируйте appsettings.json и создайте appsettings.Development.json:
```
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=articleapi;Username=postgres;Password=yourpassword"
  }
}
```
### 3. Запуск PostgreSQL (Docker)
```
docker run -d \
  --name pg-article \
  -e POSTGRES_PASSWORD=yourpassword \
  -p 5432:5432 \
  postgres:16
```

### 4. Применение миграций
```
dotnet ef database update --project ArticleApi.Infrastructure --startup-project ArticleApi.Presentation
```

### 5. Запуск API
```
dotnet run --project ArticleApi.Presentation
```
API будет доступно: https://localhost:5001 | http://localhost:5000

## 🧪 Запуск тестов

### Юнит-тесты (InMemory)
```bash
dotnet test
```
или
```
dotnet test ArticleApi.Tests
```
### Интеграционные тесты (с PostgreSQL в Docker)
Убедитесь, что Docker запущен:
```
dotnet test
```
✅ Используется Testcontainers.PostgreSql — БД создаётся и удаляется автоматически.


## 🧩 Основные сущности
| Сущность | Описание |
|--------|--------|
| `Article` | Статья с заголовком, содержанием, датой создания |
| `Tag` | Тег (например, "asp.net", "security") |
| `Section` | Секция (например, "Безопасность"), связана с тегами |
| Связь | `Article` ↔ `Tag` через `ArticleTag`, `Section` ↔ `Tag` через `SectionTag` |

> Статья попадает в секцию, если у неё есть хотя бы один общий тег.


## 🌐 API Endpoints

| Метод | Путь | Описание |
|------|------|--------|
| `GET` | `/api/articles` | Получить все статьи |
| `GET` | `/api/articles/{id}` | Получить статью по ID |
| `POST` | `/api/articles` | Создать статью |
| `PUT` | `/api/articles/{id}` | Обновить статью |
| `DELETE` | `/api/articles/{id}` | Удалить статью |
| `GET` | `/api/tags` | Получить все теги |
| `POST` | `/api/tags` | Создать тег |
| `PUT` | `/api/tags/{id}` | Обновить тег |
| `DELETE` | `/api/tags/{id}` | Удалить тег |
| `GET` | `/api/sections` | Получить все секции |
| `POST` | `/api/sections` | Создать секцию |
| `DELETE` | `/api/sections/{id}` | Удалить секцию |
| `GET` | `/api/sections/{id}/articles` | Получить статьи секции |

## 📥 Пример создания статьи

```json
POST /api/articles
Content-Type: application/json

{
  "title": "Введение в ASP.NET Core",
  "content": "Современный фреймворк от Microsoft...",
  "tagIds": [
    "a1b2c3d4-e5f6-7890-g1h2-i3j4k5l6m7n8",
    "b2c3d4e5-f6g7-8901-h2i3-j4k5l6m7n8o9"
  ]
}
```
✅ Теги должны существовать. Имя тега регистронезависимо.

## ✅ Валидация


- **Заголовок статьи**: 1–256 символов, не пустой
- **Содержание**: не пустое
- **Теги**: от 1 до 256, без дубликатов
- **ID тегов**: должны существовать в БД
- **Имя тега**: уникально (регистронезависимо), 1–256 символов
- **Порядок тегов**: сохраняется при создании/обновлении


## 🧰 Развёртывание (Docker)

### 1. Сборка образа

```bash
docker build -t artwebapi .
```

## 2.Запуск с Docker Compose
```
docker-compose up -d
```
API: http://localhost:8080
Swagger: http://localhost:8080/swagger

🔐 Пароль от БД задаётся в docker-compose.yml.

## 🛡️ Безопасность и лучшие практики

- ✅ **Регистронезависимая уникальность тегов** — нормализация: `ToLowerInvariant() + Trim()`
- ✅ **Миграции при старте** — `Database.MigrateAsync()` в `Program.cs`
- ✅ **Полное покрытие тестами** — xUnit, Moq, Testcontainers
- ✅ **Чистая архитектура** — слои не зависят от реализации
- ✅ **DTO и маппинг** — изоляция домена от API
- ⚠️ В продакшене:
  - Отключите Swagger
  - Используйте секреты вместо `appsettings.json`
  - Настройте HTTPS с Let's Encrypt


## 🧑‍💻 Автор

**Гришко Пётр Юрьевич** — разработчик API  
> 💬 Telegram: [@petkax](https://t.me/petkax)
